# Directory Scope

This file scopes `konditional-json/src/main/` and yields to deeper `AGENTS.md` files when present.

## Purpose
Production source set for JSON codecs and schema helpers.

## Go Deeper
- `kotlin/`: Kotlin sources for JSON encoding and decoding.

## Rules
- Boundary behavior belongs here; tests live in the sibling `test/` tree.
