# Directory Scope

This file scopes `konditional-json/src/test/kotlin/io/amichne/konditional/json/` and yields to deeper `AGENTS.md` files when present.

## Purpose
Public JSON entrypoint tests for round trips, strict failures, and unchanged namespace state on error.

## Local Files
- `NamespaceJsonTest.kt`

## Rules
- Add coverage here when changing `NamespaceExtensions` or the user-facing JSON boundary semantics.
