# Directory Scope

This file scopes `konditional-json/src/test/` and yields to deeper `AGENTS.md` files when present.

## Purpose
JSON codec test source set.

## Go Deeper
- `kotlin/`: Kotlin-based JSON regression tests.

## Rules
- Tests here prove round-trip correctness, strict failure behavior, and state isolation after decode errors.
