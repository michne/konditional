# Directory Scope

This file scopes `konditional-types/src/test/` and yields to deeper `AGENTS.md` files when present.

## Purpose
Types-module test source set.

## Go Deeper
- `kotlin/`: Kotlin-based foundational-type regression tests.

## Rules
- Tests here prove typed boundary behavior, identifier determinism, and context normalization semantics.
