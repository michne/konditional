# Directory Scope

This file scopes `konditional-types/src/main/` and yields to deeper `AGENTS.md` files when present.

## Purpose
Production source set for foundational types.

## Go Deeper
- `kotlin/`: Kotlin sources for shared domain types.

## Rules
- Shared type contracts belong here; tests live in the sibling `test/` tree.
