# Directory Scope

This file scopes `konditional-engine/src/test/kotlin/io/amichne/konditional/core/` and yields to deeper `AGENTS.md` files when present.

## Purpose
Tests for namespace behavior, deterministic evaluation, and namespace isolation.

## Local Files
- `NamespaceBehaviorTest.kt`

## Rules
- Add coverage here when changing namespace declaration, precedence, axis targeting, or cross-namespace isolation.
