# Directory Scope

This file scopes `konditional-engine/src/main/` and yields to deeper `AGENTS.md` files when present.

## Purpose
Production source set for engine behavior.

## Go Deeper
- `kotlin/`: Kotlin sources for the engine module.

## Rules
- Keep module behavior here; tests and fixtures live in sibling source sets.
