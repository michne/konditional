# Directory Scope

This file scopes `konditional-engine/src/testFixtures/` and yields to deeper `AGENTS.md` files when present.

## Purpose
Shared fixture source set for engine-adjacent tests.

## Go Deeper
- `kotlin/`: Shared Kotlin fixtures.

## Rules
- Keep fixtures generic and stable enough for reuse by sibling modules.
